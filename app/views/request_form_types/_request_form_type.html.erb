<tr id="<%= dom_id(request_form_type) %>" >

  <% project.main_attributes.each do |attr_name| %>
  <td><%= show_attr(request_form_type.public_send attr_name) %></td>
  <% end %>

  <td class="index_table-actions">
    <%= link_to_show(request_form_type) %>
    <%= link_to_edit(request_form_type) %>
<% if request_form_type.try(:archived?) %>
    <%= link_to_recover(request_form_type) %>
<% else %>
    <%= link_to_destroy(request_form_type) %>
<% end %>
  </td>
</tr>
