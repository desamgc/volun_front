<tr id="<%= dom_id(rft_volunteer_subscribe) %>" >

  <% project.main_attributes.each do |attr_name| %>
  <td><%= show_attr(rft_volunteer_subscribe.public_send attr_name) %></td>
  <% end %>

  <td class="index_table-actions">
    <%= link_to_show(rft_volunteer_subscribe) %>
    <%= link_to_edit(rft_volunteer_subscribe) %>
<% if rft_volunteer_subscribe.try(:archived?) %>
    <%= link_to_recover(rft_volunteer_subscribe) %>
<% else %>
    <%= link_to_destroy(rft_volunteer_subscribe) %>
<% end %>
  </td>
</tr>
