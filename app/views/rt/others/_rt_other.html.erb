<tr id="<%= dom_id(rt_other) %>" class="table-row-transform" >
  <% project.main_attributes.each do |attr_name| %>
  <td class="table-cell-group"><%= show_attr(rt_other.public_send attr_name) %></td>
  <% end %>
  <td class="table-cell-group">
    <%= link_to_show(rt_other) %>
    <%= link_to_edit(rt_other) %>
    <% if rt_other.try(:archived?) %>
    <%= link_to_recover(rt_other) %>
    <% else %>
    <%= link_to_destroy(rt_other) %>
    <% end %>
  </td>
</tr>