<tr id="<%= dom_id(rt_project_unsubscribe) %>" class="table-row-transform" >

  <% project.main_attributes.each do |attr_name| %>
  <td class="table-cell-group"><%= show_attr(rt_project_unsubscribe.public_send attr_name) %></td>
  <% end %>

  <td class="table-cell-group">
    <%= link_to_show(rt_project_unsubscribe) %>
    <%= link_to_edit(rt_project_unsubscribe) %>
<% if rt_project_unsubscribe.try(:archived?) %>
    <%= link_to_recover(rt_project_unsubscribe) %>
<% else %>
    <%= link_to_destroy(rt_project_unsubscribe) %>
<% end %>
  </td>
</tr>
